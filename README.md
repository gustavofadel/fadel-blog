# Fadel's Blog

My personal "blog" made with GatsbyJS. 

Special thanks to [João Pedro Schmitz](https://github.com/jpedroschmitz/).

## Install and run

You need to have `node` and `npm` installed in your machine. You can install it [here](https://nodejs.org/en/download/).

Then, you can clone this repository and run the following commands inside the project folder:

1. `npm install` or `yarn install`;
2. `gatsby develop`;

To view the project you can open [localhost:8000](http://localhost:8000).
